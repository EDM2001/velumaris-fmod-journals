const MODULE_ID = "velumaris-fmod-journals";

Hooks.once("init", () => {
  console.log(`${MODULE_ID} | Initialising Journals module`);
});

Hooks.once("ready", () => {
  console.log(`${MODULE_ID} | Journals module ready`);
});
